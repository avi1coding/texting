# Database Configuration
# For Render or production: use actual values from your provider (e.g., Aiven)
DB_HOST=avimehta-avimehta.k.aivencloud.com
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=texting
DB_PORT=18649

# SSL Configuration
# Set to REQUIRED to enable SSL with certificate verification
# Provide DB_SSL_CA_PATH with absolute path to CA certificate file (e.g., from Aiven)
DB_SSL=REQUIRED
# DB_SSL_CA_PATH=/path/to/ca-certificate.pem

# Or for local testing only (insecure):
# DB_SSL_INSECURE=true

# Session Secret (CHANGE THIS to a random long string! At least 32 characters)
SESSION_SECRET=change-this-to-a-very-long-random-secret-string-at-least-32-characters

# Server Port (usually 3000 locally, Render assigns automatically)
PORT=3000

# Node Environment
NODE_ENV=development
